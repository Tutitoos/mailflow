{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The local development document can access only the encrypted offline cache commands",
  "local": true,
  "windows": ["main"],
  "permissions": ["allow-offline-cache"]
}
