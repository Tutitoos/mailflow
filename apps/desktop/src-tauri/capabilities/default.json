{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the primary Mailflow window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
