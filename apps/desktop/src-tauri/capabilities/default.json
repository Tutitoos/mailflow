{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local fallback documents receive no native commands; the remote installation origin never matches this capability",
  "local": true,
  "windows": ["main"],
  "permissions": []
}
