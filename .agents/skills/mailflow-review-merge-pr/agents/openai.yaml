interface:
  display_name: "Mailflow: Review and Merge PR"
  short_description: "Review, approve, or safely merge a Mailflow PR"
  default_prompt: "Use $mailflow-review-merge-pr to review this Mailflow PR and merge it only when every gate passes."
