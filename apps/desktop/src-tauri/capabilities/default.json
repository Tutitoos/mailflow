{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The local development document can access bounded encrypted cache, native session, notification, and signed updater commands",
  "local": true,
  "windows": ["main"],
  "permissions": ["allow-offline-cache", "allow-updater"]
}
